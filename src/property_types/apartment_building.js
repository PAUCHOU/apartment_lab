var property = require("./property.js");

function ApartmentBuilding(name, address) {
  // A building can have many many units 
  var this.building = this.property.call(properties);

}

module.exports = ApartmentBuilding;